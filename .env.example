# ── Algorand Fintech Boilerplate ── Environment Configuration
# Copy this file to backend/.env and fill in your values.

# Algorand node connection (default: AlgoNode TestNet — no API key needed)
ALGORAND_ALGOD_ADDRESS=https://testnet-api.algonode.cloud
ALGORAND_ALGOD_TOKEN=

# Application environment
ENVIRONMENT=development

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:3000,http://127.0.0.1:3000
